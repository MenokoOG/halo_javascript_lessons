// ES6 Syntax: Arrow function
const shootEnemy = (enemy) => {
    return `Shooting ${enemy} with assault rifle!`;
};

// ES6 Syntax: Arrow function (shortened)
const meleeAttack = (enemy) => `Performing melee attack on ${enemy}!`;
